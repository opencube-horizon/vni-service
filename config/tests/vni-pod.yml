apiVersion: v1
kind: Pod
metadata:
  name: testpod
  annotations:
    vni: "yes"
spec:
  containers:
    - name: testpod
      image: alpine:latest
      # Just spin & wait forever
      command: [ "/bin/sh", "-c", "--" ]
      args: [ "sleep 10" ]